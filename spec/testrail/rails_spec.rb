require 'spec_helper'

describe Testrail::Rails do
  it 'has a version number' do
    expect(Testrail::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
